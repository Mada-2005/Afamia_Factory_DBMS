@page
@model FactorySystemWebpage.Pages.ProductionRooms.IndexModel
@{
    ViewData["Title"] = "Production Rooms";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="bi bi-building"></i> Production Rooms</h1>
    <a asp-page="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Add New Production Room
    </a>
</div>

<div class="row g-3">
    @* TODO: Replace with actual data from database *@
    <div class="col-md-12">
        <div class="alert alert-info">
            <i class="bi bi-info-circle"></i> No production rooms found. Connect to database to view production rooms.
        </div>
    </div>

    @* Example card structure for production rooms:
    <div class="col-md-6 col-lg-4">
        <div class="card shadow-sm h-100">
            <div class="card-header bg-success text-white">
                <h5 class="mb-0">Room 2 - Biscuit Production</h5>
            </div>
            <div class="card-body">
                <p><strong>Type:</strong> Biscuits</p>
                <p><strong>Location:</strong> Building B</p>
                <p><strong>Status:</strong> <span class="badge bg-success">Active</span></p>
                <p><strong>Production Lines:</strong> 3</p>
                <p class="mb-0"><small class="text-muted">Line 1, Line 2, Line 3</small></p>
            </div>
            <div class="card-footer">
                <div class="d-flex gap-2">
                    <a asp-page="Details" asp-route-id="1" class="btn btn-sm btn-outline-info">
                        <i class="bi bi-eye"></i> View
                    </a>
                    <a asp-page="Edit" asp-route-id="1" class="btn btn-sm btn-outline-primary">
                        <i class="bi bi-pencil"></i> Edit
                    </a>
                    <a asp-page="Delete" asp-route-id="1" class="btn btn-sm btn-outline-danger">
                        <i class="bi bi-trash"></i> Delete
                    </a>
                </div>
            </div>
        </div>
    </div>
    *@
</div>

<div class="mt-3">
    <a asp-page="/Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to Home
    </a>
</div>
