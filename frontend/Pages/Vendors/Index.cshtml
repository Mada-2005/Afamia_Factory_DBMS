@page
@model FactorySystemWebpage.Pages.Vendors.IndexModel
@{
    ViewData["Title"] = "Vendors";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="bi bi-truck"></i> Vendors</h1>
    <a asp-page="Create" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Add New Vendor
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th>Company Name</th>
                        <th>Contact Person</th>
                        <th>Phone</th>
                        <th>Email</th>
                        <th>Address</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @* TODO: Replace with actual data from database *@
                    <tr>
                        <td colspan="6" class="text-center text-muted">
                            <i class="bi bi-info-circle"></i> No vendors found. Connect to database to view vendors.
                        </td>
                    </tr>
                    @* Example row structure:
                    <tr>
                        <td>ABC Foods Company</td>
                        <td>John Smith</td>
                        <td>+20-123-4567890</td>
                        <td>john@abcfoods.com</td>
                        <td>Cairo Industrial Zone</td>
                        <td>
                            <a asp-page="Edit" asp-route-id="1" class="btn btn-sm btn-outline-primary">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <a asp-page="Details" asp-route-id="1" class="btn btn-sm btn-outline-info">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a asp-page="Delete" asp-route-id="1" class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i>
                            </a>
                        </td>
                    </tr>
                    *@
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="mt-3">
    <a asp-page="/Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Back to Home
    </a>
</div>
